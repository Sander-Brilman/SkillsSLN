<dialog popover open="@IsOpen">
    @ChildContent
</dialog>

@code {
    [Parameter, EditorRequired]
    public bool IsOpen { get; set; }

    [Parameter]
    public EventCallback<bool> IsOpenChanged { get; set; }

    [Parameter, EditorRequired]
    public RenderFragment ChildContent { get; set; }
}
